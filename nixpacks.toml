[phases.install]
cmds = [
  "npm install",
  "pip install fastapi uvicorn 'pydantic[email]' python-dotenv email-validator"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "uvicorn backend.server:app --host 0.0.0.0 --port ${PORT:-3004}"
